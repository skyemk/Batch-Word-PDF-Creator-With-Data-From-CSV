# Batch-Word-PDF-Creator-With-Data-From-CSV
This project provides a Python script to automate the generation of personalized Word documents (.docx) from a reusable template using data from a CSV file. It also conditionally converts selected documents into PDF format (.pdf) based on a specified column in the input data, using LibreOffice to handle reliable, automated document conversion fast.
